# Essential requirements for Skryper development

# PyInstaller for building the executable
pyinstaller

# Unit testing and mocking library
unittest2>=1.1.0

# Path handling (already part of Python, but including for explicit dependency handling)
pathlib>=1.0.1

# Used for mocking in tests
mock>=4.0.3

# Optional: coverage tool for measuring test coverage
coverage>=6.4.1
